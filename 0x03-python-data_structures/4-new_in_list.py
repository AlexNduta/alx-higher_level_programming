#!/usr/bin/python3

def new_in_list(my_list, idx, element):
    length = len(my_list)
    copied_list = my_list.copy()
    if my_list != []:
        if idx < 0:
            return cp
        elif idx >= length:
            return cp1
        else:
            copied_list[idx] = element
            return copied_list
